<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns:fx="http://javafx.com/fxml/1" 
    fx:controller="com.dat.notebook.controller.AiChatController" 
    styleClass="ai-chat-root" 
    stylesheets="@../css/soft-peach.css" 
    prefWidth="520" prefHeight="650">

    <!-- Header -->
    <top>
        <VBox styleClass="ai-chat-header" spacing="8">
            <padding><Insets top="15" right="20" bottom="15" left="20"/></padding>
            
            <HBox alignment="CENTER_LEFT" spacing="12">
                <Label text="🤖" style="-fx-font-size: 32px;"/>
                <VBox spacing="2">
                    <Label text="AI Assistant" styleClass="ai-chat-title"/>
                    <Label text="Tóm tắt • Gợi ý tiêu đề • Gợi ý tags" styleClass="ai-chat-subtitle"/>
                </VBox>
                <Region HBox.hgrow="ALWAYS"/>
                <ProgressIndicator fx:id="loadingIndicator" visible="false" prefWidth="24" prefHeight="24"/>
                <Button text="✕" styleClass="close-btn" onAction="#handleClose"/>
            </HBox>
            
            <!-- Quick Action Buttons -->
            <HBox spacing="10" alignment="CENTER">
                <Button text="📝 Tóm tắt" styleClass="ai-quick-action-btn" onAction="#handleSummarize">
                    <tooltip><Tooltip text="Tóm tắt ghi chú thành 3-5 điểm chính"/></tooltip>
                </Button>
                <Button text="💡 Gợi ý tiêu đề" styleClass="ai-quick-action-btn" onAction="#handleSuggestTitle">
                    <tooltip><Tooltip text="Gợi ý tiêu đề phù hợp cho ghi chú"/></tooltip>
                </Button>
                <Button text="🏷️ Gợi ý tags" styleClass="ai-quick-action-btn" onAction="#handleSuggestTags">
                    <tooltip><Tooltip text="Gợi ý 3-7 tags để phân loại ghi chú"/></tooltip>
                </Button>
            </HBox>
        </VBox>
    </top>

    <!-- Chat Messages -->
    <center>
        <ScrollPane fx:id="chatScrollPane" fitToWidth="true" styleClass="chat-scroll-pane">
            <VBox fx:id="chatContainer" spacing="15" styleClass="chat-container">
                <padding><Insets top="20" right="20" bottom="20" left="20"/></padding>
            </VBox>
        </ScrollPane>
    </center>

    <!-- Input Area -->
    <bottom>
        <VBox styleClass="chat-input-area" spacing="8">
            <padding><Insets top="15" right="20" bottom="15" left="20"/></padding>
            
            <HBox alignment="CENTER" spacing="10">
                <TextField fx:id="messageInput" 
                    promptText="Nhập lệnh: tóm tắt, tiêu đề, tag..." 
                    styleClass="chat-input-field" 
                    HBox.hgrow="ALWAYS" 
                    onAction="#handleSendMessage"/>
                <Button fx:id="sendButton" 
                    text="➤" 
                    styleClass="chat-send-btn" 
                    onAction="#handleSendMessage"/>
            </HBox>
            
            <Label text="💡 Tip: Chọn một ghi chú trước, sau đó nhấn các nút phía trên hoặc nhập lệnh." 
                styleClass="chat-tip-label"/>
        </VBox>
    </bottom>
</BorderPane>
