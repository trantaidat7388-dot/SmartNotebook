================================================================
  SMART NOTEBOOK - HƯỚNG DẪN ĐẦY ĐỦ
================================================================

📘 Tài liệu này chứa TẤT CẢ hướng dẫn về SmartNotebook:
   • Cài đặt & chạy ứng dụng (cho người dùng)
   • Build từ source code (cho developers)
   • Build offline không cần internet
   • Troubleshooting & support

================================================================
  PHẦN 1: CHO NGƯỜI DÙNG CUỐI (END-USER)
================================================================

1.1. YÊU CẦU HỆ THỐNG
----------------------
✅ Hệ điều hành: Windows 10/11, Linux, macOS
✅ Java Runtime Environment (JRE): Version 17 trở lên
✅ RAM: Tối thiểu 2GB (Khuyến nghị 4GB+)
✅ Database: Microsoft SQL Server 2014 trở lên

1.2. KIỂM TRA JAVA
------------------
1. Mở Command Prompt (Windows) hoặc Terminal (Linux/Mac)
2. Chạy lệnh: java -version
3. Nếu chưa cài đặt Java 17+, tải tại: https://adoptium.net/

1.3. CÀI ĐẶT & CẤU HÌNH DATABASE
---------------------------------
1. Cài đặt Microsoft SQL Server nếu chưa có
   - Tải SQL Server Express (miễn phí): 
     https://www.microsoft.com/sql-server/sql-server-downloads
   
2. Khởi động SQL Server

3. Import database schema:
   - Mở file: database/database.sql
   - Execute toàn bộ script trong SQL Server Management Studio
   - Hoặc chạy lệnh:
     sqlcmd -S localhost -U sa -P YourPassword -i database/database.sql

4. Tạo user cho ứng dụng (optional):
   CREATE LOGIN smartnotebook_user WITH PASSWORD = 'your_password';
   USE SmartNotebook;
   CREATE USER smartnotebook_user FOR LOGIN smartnotebook_user;
   GRANT ALL PRIVILEGES ON DATABASE SmartNotebook TO smartnotebook_user;

1.4. CẤU HÌNH ỨNG DỤNG
-----------------------
1. Mở file: config/config.properties

2. Cập nhật thông tin kết nối database:
   
   db.host=localhost          # Địa chỉ SQL Server
   db.port=1433              # Port mặc định
   db.name=SmartNotebook     # Tên database
   db.user=sa                # Username
   db.password=YourPassword  # Password

3. Lưu file

1.5. CHẠY ỨNG DỤNG
------------------
Windows:
  • Double-click: run.bat
  • Hoặc: java -jar SmartNotebook.jar

Linux/Mac:
  • Terminal: java -jar SmartNotebook.jar

1.6. TROUBLESHOOTING CHO END-USER
---------------------------------
❌ LỖI: "Java is not recognized"
   → Cài đặt Java JRE 17+ và thêm vào PATH

❌ LỖI: "Connection refused" 
   → Kiểm tra SQL Server đã chạy chưa
   → Kiểm tra port 1433 có bị firewall chặn không
   → Xác nhận username/password trong config.properties đúng

❌ LỖI: "Login failed for user"
   → Kiểm tra SQL Server Authentication mode (phải bật Mixed Mode)
   → Reset password user trong SQL Server Management Studio

❌ LỗI: "Could not find or load main class"
   → Chạy từ thư mục chứa file SmartNotebook.jar
   → Kiểm tra file JAR không bị corrupt

❌ LỖI: "JavaFX runtime components are missing"
   → Sử dụng file SmartNotebook.jar (Fat JAR có sẵn JavaFX)

================================================================
  PHẦN 1A: SETUP VỚI DOCKER 🐋 (DỄ HƠN!)
================================================================

1A.1. DOCKER LÀ GÌ?
-------------------
Docker giúp chạy SQL Server trong container - KHÔNG CẦN cài SQL Server!

1A.2. YÊU CẦU
-------------
✅ Java JDK 17+
✅ Docker Desktop: https://www.docker.com/products/docker-desktop/

1A.3. SETUP (CHỈ 3 BƯỚC!)
--------------------------

Bước 1 - Start SQL Server Container:
  
  docker-start.bat
  
  → Tự động start SQL Server trong Docker (~2 phút)

Bước 2 - Initialize Database:
  
  docker-init-db.bat
  
  → Tự động import database schema

Bước 3 - Run Application:
  
  run.bat
  
  → DONE! Ứng dụng kết nối tới Docker SQL Server

1A.4. DOCKER COMMANDS
---------------------

Start database:
  docker-start.bat              # Start SQL Server

Stop database:
  docker-stop.bat               # Stop containers

View status:
  docker ps                     # Check containers running

View logs:
  docker logs smartnotebook-db  # See SQL Server logs

1A.5. CONNECTION INFO
---------------------
Khi dùng Docker:
  Host: localhost
  Port: 1433
  Username: sa
  Password: SmartNotebook@2024

File config.properties đã auto đúng!

1A.6. TROUBLESHOOTING DOCKER
----------------------------

❌ "Docker is not running"
   → Mở Docker Desktop application

❌ "Port 1433 already in use"
   → Stop SQL Server trên Windows: net stop MSSQLSERVER
   → Hoặc đổi port trong docker-compose.yml

❌ "Cannot connect to database"
   → Check container: docker ps
   → Init database: docker-init-db.bat

1A.7. SO SÁNH: TRADITIONAL VS DOCKER
------------------------------------

Traditional Setup:
  1. Download SQL Server Express (1.5GB)
  2. Install (~15-30 min)
  3. Configure authentication
  4. Create database
  5. Import schema manually
  → Total: ~45 minutes

Docker Setup:
  1. docker-start.bat (2 min)
  2. docker-init-db.bat (1 min)
  3. run.bat
  → Total: ~3 minutes ✨

→ Docker DỄ HƠN, NHANH HƠN! 🚀


================================================================
  PHẦN 2: CHO DEVELOPERS - BUILD TỪ SOURCE
================================================================

2.1. YÊU CẦU CHO BUILD
----------------------
✅ Java JDK 17+ (phải là JDK, không phải JRE)
✅ Apache Maven 3.6+
✅ Internet (cho online build) HOẶC Source Distribution Package (cho offline)

Kiểm tra:
  java -version      # Phải version 17+
  javac -version     # Compiler phải có
  mvn -version       # Maven 3.6+

2.2. BUILD DISTRIBUTION PACKAGE (CÓ INTERNET)
----------------------------------------------

Option 1 - Sử dụng Script:
  
  Windows:
    build-distribution.bat

Option 2 - Maven Command:
  
  mvn clean package

Kết quả:
  • target/SmartNotebook.jar                           (~41MB Fat JAR)
  • target/SmartNotebook-1.0-SNAPSHOT-distribution.zip (~40MB)

2.3. CẤU TRÚC DISTRIBUTION PACKAGE
-----------------------------------
SmartNotebook-1.0-SNAPSHOT/
├── SmartNotebook.jar          # Fat JAR chính
├── run.bat, setup.bat         # Scripts
├── SETUP_GUIDE.txt            # File hướng dẫn này
├── config/
│   └── config.properties      # Database config
├── database/
│   └── database.sql           # SQL schema
├── resources/
│   ├── ai_prompts_offline.txt
│   └── nlp/vietnamese-stopwords.txt
└── docs/
    ├── README.md
    └── LICENSE

2.4. PHÂN PHỐI CHO END-USER
---------------------------
1. Build distribution: mvn clean package
2. Lấy file: target/SmartNotebook-1.0-SNAPSHOT-distribution.zip
3. Upload lên GitHub Releases / Google Drive / etc
4. Gửi cho user kèm file hướng dẫn này


================================================================
  PHẦN 3: BUILD OFFLINE (KHÔNG CẦN INTERNET)
================================================================

3.1. TỔNG QUAN OFFLINE BUILD
-----------------------------
SmartNotebook hỗ trợ build từ source code HOÀN TOÀN KHÔNG CẦN INTERNET
bằng cách bundle Maven repository với tất cả dependencies.

Khi nào cần?
  ✅ Môi trường không có internet (air-gapped)
  ✅ Để đảm bảo reproducible builds
  ✅ Corporate environments với restricted internet
  ✅ Faster builds (không download)

3.2. TẠO SOURCE DISTRIBUTION PACKAGE (CHO DEVELOPER)
----------------------------------------------------

Bước 1 - Bundle Maven Repository:
  
  bundle-maven-repo.bat
  
  → Download tất cả dependencies vào target/maven-repo/

Bước 2 - Build Source Distribution:
  
  build-source-distribution.bat
  
  → Tạo file: target/SmartNotebook-1.0-SNAPSHOT-source.zip (~150MB)

Package này chứa:
  • Source code đầy đủ
  • Maven repository với TẤT CẢ dependencies
  • Build scripts offline
  • Documentation

3.3. BUILD OFFLINE (CHO USER NHẬN SOURCE PACKAGE)
--------------------------------------------------

Bước 1 - Giải nén:
  
  unzip SmartNotebook-1.0-SNAPSHOT-source.zip
  cd SmartNotebook-1.0-SNAPSHOT-source/

Bước 2 - Build Offline:
  
  Windows:
    build-offline.bat
  
  Manual Maven:
    mvn clean package -o -Dmaven.repo.local=maven-repository

  Parameters:
    -o : Offline mode (không download)
    -Dmaven.repo.local=maven-repository : Dùng bundled repo

Bước 3 - Kết quả:
  
  • target/SmartNotebook.jar
  • target/SmartNotebook-1.0-SNAPSHOT-distribution.zip

3.4. SO SÁNH DISTRIBUTION TYPES
--------------------------------

┌─────────────────────────────────────────────────────────┐
│ Binary Distribution (cho end-user)                      │
│ Size: ~40MB | Cần: Java 17+ | Internet: Không          │
│ Use: Chạy ứng dụng trực tiếp                           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Source Code (Git clone)                                 │
│ Size: ~5MB | Cần: JDK 17+, Maven, Internet             │
│ Use: Developers có internet                             │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ Source Distribution (Offline)                           │
│ Size: ~150MB | Cần: JDK 17+, Maven | Internet: Không   │
│ Use: Build hoàn toàn offline                            │
└─────────────────────────────────────────────────────────┘

3.5. BUILD PERFORMANCE
----------------------
┌──────────────┬──────────────┬────────────────┬──────────┐
│ Build Type   │ First Build  │ Subsequent     │ Internet │
├──────────────┼──────────────┼────────────────┼──────────┤
│ Online       │ 2-3 min      │ 30-60 sec      │ Required │
│ Offline      │ 30-60 sec    │ 30-60 sec      │ No       │
└──────────────┴──────────────┴────────────────┴──────────┘

Offline build nhanh hơn 3-4x!

3.6. TROUBLESHOOTING OFFLINE BUILD
-----------------------------------

❌ LỖI: "Cannot resolve dependencies" (offline mode)
   → Maven repository không đủ dependencies
   → Rebuild: bundle-maven-repo.bat
   → Thử lại: build-offline.bat

❌ LỖI: "Repository not found"
   → Kiểm tra thư mục maven-repository/ tồn tại
   → Verify: dir maven-repository\org\openjfx

❌ LỖI: "mvn: command not found"
   → Cài Maven hoặc dùng Maven Wrapper:
     Windows: mvnw.cmd clean package -o -Dmaven.repo.local=maven-repository
     Linux: ./mvnw clean package -o -Dmaven.repo.local=maven-repository


================================================================
  PHẦN 4: MAVEN PLUGINS & TECHNICAL DETAILS
================================================================

4.1. FAT JAR (Maven Shade Plugin)
----------------------------------
Fat JAR chứa:
  • Application code
  • TẤT CẢ dependencies (JavaFX, JDBC, OpenNLP)
  • Resources (FXML, CSS, NLP data)

Lợi ích:
  ✅ 1 file duy nhất
  ✅ Không cần cài thêm thư viện
  ✅ Cross-platform

Build:
  mvn clean package
  → Output: target/SmartNotebook.jar (~41MB)

4.2. DEPENDENCIES INCLUDED
---------------------------
• JavaFX 21
  - javafx-controls
  - javafx-fxml
  - javafx-web

• Database
  - mssql-jdbc 7.4.1.jre8

• NLP
  - Apache OpenNLP 2.3.0

4.3. CUSTOM ASSEMBLY DESCRIPTORS
---------------------------------
Distribution assembly: src/assembly/distribution.xml
Source assembly: src/assembly/source-distribution.xml

Customize bằng cách edit XML files này.


================================================================
  PHẦN 5: TESTING & RELEASE CHECKLIST
================================================================

5.1. TESTING DISTRIBUTION PACKAGE
----------------------------------
1. Giải nén vào thư mục mới
2. Cấu hình database
3. Chạy run.bat hoặc java -jar SmartNotebook.jar
4. Kiểm tra:
   - [ ] Application khởi động OK
   - [ ] Kết nối database OK
   - [ ] Tạo/sửa/xóa note hoạt động
   - [ ] AI features hoạt động
   - [ ] UI hiển thị đúng

5.2. RELEASE CHECKLIST
----------------------
Trước khi release:
  - [ ] Update version trong pom.xml
  - [ ] Update hướng dẫn với thông tin hỗ trợ
  - [ ] Test trên Windows 10/11
  - [ ] Test với SQL Server
  - [ ] Kiểm tra file size (<100MB cho binary)
  - [ ] Tạo Git tag cho version
  - [ ] Build final: mvn clean package
  - [ ] Upload lên GitHub Releases
  - [ ] Update README.md với link download

5.3. OPTIMIZATION TIPS
----------------------
Giảm dung lượng JAR:
  mvn dependency:analyze    # Tìm unused dependencies

Tăng tốc startup:
  java -Xmx512m -XX:+UseG1GC -jar SmartNotebook.jar


================================================================
  HỖ TRỢ & LIÊN HỆ
================================================================

📧 Email: [Thêm email của bạn]
🐛 Report bugs: [GitHub Issues link]
📖 Documentation: README.md trong thư mục docs/
💬 Support: [Thêm link support]

================================================================
  GIẤY PHÉP
================================================================

Xem file LICENSE trong thư mục docs/

================================================================

Cảm ơn bạn đã sử dụng SmartNotebook! 📓✨

Build anywhere, run everywhere!
